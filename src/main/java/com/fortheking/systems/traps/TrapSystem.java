package com.fortheking.systems.traps;

import com.fortheking.core.engine.ILevelSystem;
import net.minecraft.server.level.ServerLevel;

public class TrapSystem implements ILevelSystem {

    @Override
    public void tickLevel(ServerLevel level) {
        // TODO: Handle bear trap timers, interactions, etc.
    }
}
